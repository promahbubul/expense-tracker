{
  "name": "expense-tracker-suite",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @expense-tracker/api\" \"npm run dev -w @expense-tracker/web\"",
    "build": "npm run build -w @expense-tracker/api && npm run build -w @expense-tracker/web",
    "lint": "npm run lint -w @expense-tracker/api && npm run lint -w @expense-tracker/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
